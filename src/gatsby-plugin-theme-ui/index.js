import { merge } from "theme-ui";
import originalTheme from "@devpanther/gatsby-theme-minimal-blog/src/gatsby-plugin-theme-ui/index";

const theme = merge(originalTheme, {
  
});

export default theme;